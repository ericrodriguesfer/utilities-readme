################### Install library ###################
npm install readline-sync

################### Code ###################
var readLineSync = require('readline-sync');
let getLine = () => readLineSync.question();

let nome = String(getLine());

console.log(nome);
